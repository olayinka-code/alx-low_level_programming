#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _puts_recursion - prints a string followed by newline to the stdout.
 * @s: a pointer to the string
 *
 * Return: always 0 (succes)
 */
void _puts_recursion(char *s)
{
	puts(s);
}
